// const fs = require('fs/promises');

// Promises API
// (async () => {
//   try {
//     await fs.copyFile('./text.txt', './coppied-text.txt');
//   } catch (error) {
//     console.log('ASYNC ERROR----->', error);
//   }
// })();

// Callback API
// const fs = require('fs');
// fs.copyFile('./text.txt', './coppied-text.txt', (error) => {
//   if (error) console.log('CB ERROR ----->', error);
// });

// Sync API
